
use std::u32::MAX;

use basic;

// MedianString(Dna, k)
//     distance ← ∞
//     for each k-mer Pattern from AA…AA to TT…TT
//         if distance > d(Pattern, Dna)
//              distance ← d(Pattern, Dna)
//              Median ← Pattern
//     return Median

pub fn median_string(k: i32, dna: Vec<&str>) {
    let distance: u32 = MAX;



    return;
}
